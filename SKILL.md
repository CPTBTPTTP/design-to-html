---
name: design-to-html
description: 设计稿还原助手：将第三方提供的设计图（PNG/JPG/Figma/Sketch，1~N 张）系统性还原为开发可用的 HTML 文件，还原度目标 ≥90%。覆盖全局设计标准提取、单页视觉拆解、SVG/位图素材整理、HTML 结构与样式还原、还原度量化验收六步流水线。触发词：设计图还原、设计稿转HTML、还原成HTML、图片转网页、设计稿切图、一比一还原、设计还原、还原度。
version: 1.0.0
agent_created: true
---

# 设计稿还原（Design → HTML）

将第三方提供的设计图（PNG/JPG/Figma/Sketch，1~N 张）系统性还原为开发可用的 HTML 文件，还原度目标 ≥90%。核心方法：**标注先行**（还原质量在标注/拆解阶段就已决定，HTML 只是机械翻译）、**一图一档**（每个页面一份视觉拆解单，作为该页唯一作业依据）、**门槛放行**（每个阶段设质量门槛 Gate，不达标不放行，从源头消灭返工）。

## 总原则

1. **标注先行**：还原质量在「解析 / 拆解 / 标注」阶段就已决定，后续 HTML/CSS 只是机械翻译。标注阶段的耗时占比约 50%，不可压缩。
2. **一图一档**：每个页面产出独立的「视觉拆解单」，作为该页的唯一作业依据与验收对照。
3. **门槛放行**：六个阶段各设质量门槛（G0–G5），不达标必须回退补全，禁止带病进入下一阶段。
4. **素材分层**：SVG 只承接「可被路径精确描述的规则几何」（图标/Logo/色块/规则渐变）；「有机材质」（手写字、水彩插画、纹理、摄影）一律用位图，不强行转 SVG。

## 触发与边界

**正向触发**：用户提供 ≥1 张设计图，且意图为「还原为 HTML / 页面 / 组件」。

**不触发 / 转交**：
- 明确要求 `.pptx` 文件 → 转交 PPT 类 skill。
- 纯咨询、只要流程不落地 → 不触发，仅回答。
- 非设计任务（后端、数据、算法等）→ 拒绝并引导回设计领域。
- 针对 Ardot 画布设计稿的「设计→代码」→ 走 `ardot-design-to-code`，本 skill 针对第三方位图/源文件设计图。

**前置校验**：图片可读、数量 ≥1、格式合法（PNG/JPG/Figma/Sketch/PSD）。无法读取或数量为 0 → 终止并提示补齐。

## 输入契约

| 输入 | 是否必需 | 默认值 | 说明 |
|---|---|---|---|
| 设计图 | ✅ 必需 | — | PNG/JPG/Figma/Sketch/PSD，1~N 张 |
| 还原度阈值 | 可选 | 90% | 验收达标线 |
| 目标设备/断点 | 可选 | 桌面+移动 | 响应式范围 |
| 优先级标注 | 可选 | 全部 P0 | 决定执行顺序 |
| 素材源形态 | 建议必需 | — | 透明底 PNG（@2x）/ 可分离分层源文件 vs 整图 JPG，决定素材处理方式与还原度上限 |

### 前置校验规则 P1（素材源形态校验）

1. **素材扫描**：盘点时对每张图自动扫描，识别是否含「手写字 / 水彩插画 / 纹理 / 摄影」四类位图素材。
2. **源文件需求清单**：若含位图素材，输出「素材源文件需求清单」，向第三方要求提供透明底 PNG（推荐 @2x）或可分离的分层源文件（Figma/Sketch/PSD）。
3. **降级处理**：若第三方只能提供整图 JPG/PNG，触发「抠图降级流程」——允许自动抠图，但须在 T6 问题清单自动生成一条 P1 级风险项（`[素材名] 由整图抠取，边缘还原度可能下降`），并可将该页面还原度目标协商下调（如 90%→85%），记录到 T5 清单备注。

## 执行编排（六步流水线）

```
Step1 收集盘点 ─G0→ Step2 全局定标 ─G1→ Step3 单页拆解 ─G2→
Step4 结构搭建 ─G3→ Step5 样式还原 ─G4→ Step6 还原验收 ─G5→ 交付
```

**执行模式分支**（按输入规模选择，决定并行度）：

| 规模 | 执行模式 | 行为 |
|---|---|---|
| 1 张 | 单次直跑 | Step1→6 顺序走一遍，全局定标轻量化（直接标注进单页，不单独建全局文档） |
| 2–5 张 | 简版并行 | Step2 建简版全局标准，Step3–6 逐页循环 |
| 6 张以上 | 批量流水线 | Step2 只做一次，Step3–6 多页并行 |

### Step 1｜收集与盘点
- 清点数量、核对版本、确认目标设备/分辨率/断点、判断「整图」还是「分层稿」。
- 执行前置校验规则 P1。
- **输出**：T5 设计稿清单。
- **Gate G0**：数量、版本、范围无歧义，整图/分层与素材源形态已确认。

### Step 2｜全局标准提取
- 抽样提取公共 Token：色彩、字体、间距、圆角、阴影、图标、组件样式。
- **输出**：T1 全局设计标准文档（全站唯一真值来源）。
- **Gate G1**：核心 Token（色/字/组件）覆盖所有页面，无遗漏。

### Step 3｜单页视觉拆解（标注核心，耗时约 50%）
- 布局分块编号 → 组件识别 → 逐元素标注样式 → 偏离全局规范项高亮 → 按「素材格式判定决策树」判定并导出 SVG/位图素材。
- **输出**：T2 单页视觉拆解单 + T3 命名的 SVG/位图素材。
- **Gate G2**：图上每个像素区域都能归属到某组件/样式规则，**零「未标注盲区」**。

### Step 4｜HTML 结构搭建
- 划分语义结构（header/main/section）→ 定布局方案（Flex/Grid）→ 搭组件骨架。
- **输出**：T7 规范的 HTML 结构草案（带注释）。
- **Gate G3**：结构与拆解单的区块编号一一对应。

### Step 5｜样式还原与开发
- Token 落地为 CSS 变量 → 逐块还原样式 → 交互/动效 → 响应式适配。
- **输出**：HTML/CSS 成品 + 素材，遵循 T8 前端代码规范。
- **Gate G4**：本地叠图自查通过。

### Step 6｜还原度验收
- 像素级叠图对比 → 按 T4 评分表打分 → 生成 T6 问题清单 → 修复闭环。
- **输出**：T4 还原度评分表 + T6 问题修复清单。
- **Gate G5**：综合 ≥90 分，且「布局」「色彩」单项达标。

## 素材格式判定决策树（Step3 核心，确定性规则，消除「视情况」模糊）

**判定输入特征**（自动检测 5 个维度）：

| 特征 | 取值 | 检测方式 |
|---|---|---|
| F1 边缘类型 | 硬边缘（几何）/ 软边缘（晕染、羽化、渐变过渡） | 边缘梯度分析 |
| F2 结构规律 | 规则几何 / 有机随机 | 形状识别 |
| F3 色调类型 | 纯色·少色 / 连续色调·多色阶 | 色彩直方图 |
| F4 材质感 | 有（笔触/纸纹/噪点/晕染）/ 无 | 纹理检测 |
| F5 透明需求 | 需透明底 / 无需 | 用途判断 |

**决策流程**（顺序执行，首个命中即停）：

```
Step0（仅源文件）是否仍为矢量文本/矢量图层？
   └─ 是 → SVG（真矢量，还原度 100%）
   └─ 否 → 继续
Step1 是否为规则几何图形（图标/Logo/色块/规则渐变）？
   └─ 是 → 纯色块用 CSS；其余用 SVG
   └─ 否 → 继续
Step2 是否为照片（连续色调、真实摄影）？
   └─ 是 → 需透明：PNG-24 / WebP；无需透明：JPEG / WebP
   └─ 否 → 继续
Step3 是否含材质/晕染/随机纹理（手写字、水彩、纹理、噪点）？
   └─ 是 → 透明 PNG-24 / WebP（纹理按可平铺切片）
   └─ 否 → 默认 SVG
```

**判定结果 → 导出参数对照表**：

| 命中 | 输出格式 | 导出参数 |
|---|---|---|
| 矢量文本/图层 | SVG | 文字转轮廓、viewBox 保留 |
| 规则几何（非纯色） | SVG | 描边转轮廓、坐标精度 1–2 位小数、透明背景 |
| 纯色块 | CSS | 不导出，用 CSS 变量实现 |
| 手写字/水彩/纹理（位图） | PNG-24 / WebP | 透明底、无损、原始尺寸、另出 @2x/@3x |
| 摄影（需透明） | PNG-24 / WebP | 透明底、质量 85–90 |
| 摄影（无需透明） | JPEG / WebP | 质量 80–90、sRGB |
| 复杂位图滤镜 | PNG / WebP | 透明按需 |

**口诀**：看得见「形状」的用 SVG，看得见「材质」的用位图。

## 验收标准（90% 还原度量化）

- 评分体系见 T4 表，综合得分 ≥90 达标。
- **红线项**：布局、色彩两维度单项加权后不得低于各自满分的 90%。
- **修复闭环**：验收失败 → 生成 T6 问题清单 → 修复 → 复验，最多循环 3 轮；仍不达标则升级人工决策并说明卡点。

## 失败处理与边界

| 异常场景 | 处理策略 |
|---|---|
| 设计图缺失/损坏 | 终止运行，明确列出缺什么 |
| 整图（无分层源文件） | 提示标注成本上升，征询是否继续 |
| 非设计任务输入 | 拒绝并引导到正确能力 |
| 验收不达标 | 进入修复闭环，最多 3 轮 |
| 素材无法导出 | 降级为位图并记录到 T6 问题清单 |
| 位图素材由整图抠取 | 触发 P1 降级流程，标记还原度可能下降 |

## 依赖能力

| 能力 | 用途 | 对应步骤 |
|---|---|---|
| 读图/测量 | 尺寸、间距、栅格测量 | Step2–3 |
| 色彩取样 | Token 提取 | Step2 |
| 字体识别 | 字体族/字号识别 | Step2–3 |
| SVG 导出/优化 | 素材提取 | Step3 |
| HTML/CSS 生成 | 结构搭建与样式还原 | Step4–5 |
| 截图对比 | 叠图验证还原度 | Step6 |

## 参考文件

- `references/templates.md`：8 个成果物模板（T1–T8）的完整字段结构，生成任何成果物前先加载。
