---
name: design-to-html
description: 将第三方设计图还原为可开发的 HTML。先对全部页面快速分级，再按页面复杂度选择标准还原或精修验收；适用于截图、PNG/JPG、Figma、Sketch、PSD，不用于纯流程咨询或后端任务。
version: 1.1.0
agent_created: true
---

# 设计稿还原（Design → HTML）

目标是交付可维护的 HTML/CSS/素材，而不是把整张设计图铺成背景。所有可变的名称、日期、金额、进度、状态、列表与交互控件必须保留为 DOM 和数据字段；稳定且不交互的装饰可以使用经确认的素材。

默认采用**标准还原**：先一次性看完全部设计图、为页面分级，再把精修成本投入到真正需要的页面。`≥90%` 是进入精修验收后、在相同视口与相同状态下的目标，不是每次拿到截图就自动承诺的工作量。

## 模式与选择

用户没有明确指定时，使用「标准还原」。不要为了看懂 1–2 张图而创建完整设计系统、逐像素标注或循环截图。

| 模式 | 适用场景 | 默认产出 | 成本边界 |
|---|---|---|---|
| 快速评估 | 用户只想判断可行性、排优先级或报价 | T0 页面分级与风险 | 不建页面、不做素材、不写代码 |
| 标准还原（默认） | 常规原型、2–5 张图、首轮开发 | T0、必要的 T1/T2/T3、HTML、一次对比记录 | 每页一次视觉拆解；只处理首屏/核心流程和必要素材；一次比对后报告是否值得精修 |
| 精修验收 | 用户明确指定核心页，或要求以 90%+ 验收 | 完整 T1–T4 与问题闭环 | 逐层拆解、素材优化、同视口复验；由用户确认后才开始 |

### 页面分级

先为输入中的每一页标为 A/B/C，并让用户看到结果后再开始高成本工作：

- **A · 视觉锚点/精修候选**：首页、关键转化或交接流程、独有插画/拼贴/特殊字体、需要客户验收的页面。
- **B · 标准还原**：普通详情、列表、表单；严格使用已确认的 Token 和组件，但不默认进行多轮像素微调。
- **C · 组件复用**：和已实现页面高度重复的状态页/列表页；复用结构和组件，只校验内容、状态和本页差异。

“页面简单”不等于不还原；它只是不需要先消耗精修成本。若用户指定页面优先级，以用户指定为准。

## 轻量工作流

```
输入设计图 → T0 快速盘点/分级 → 共享元素确认 → 页面拆解 → 素材与 HTML → 一次同视口对比 → 决定是否精修
```

### 0. 快速盘点与范围确认（全量、一次）

- 记录每张图的文件、画布尺寸、页面/状态、源文件形态和目标路由；先用设计图本身的画布作为还原视口。
- 标出共用组件、独有视觉资产、动态业务字段和缺失素材风险，并给出 A/B/C 分级。
- 默认不推断未提供的桌面端、移动端、悬停态、动效或业务流程。需要其它断点时，要求相应设计图或把它记录为假设。
- **输出 T0**。只有 3 张以上且确实存在共用样式时才创建独立 T1；否则将少量 Token 写在对应 T2 中。

### 1. 共享设计标准（按需）

- 从多页重复出现的颜色、字号、间距、圆角、阴影和组件提取可复用 Token；不凭感觉补齐设计稿没有的组件状态。
- T1 是项目中共享 Token 的唯一来源。若页面数量少或无复用价值，不单独创建 T1。

### 2. 单页拆解与素材判定

- 每页一份紧凑 T2：只覆盖可见的功能区、可复用组件、页面特例、数据/交互边界和素材层；不要求“每个像素都有标注”。
- 优先把精力放在会显著影响感知的区域：页面骨架、标题层级、卡片比例、留白、主视觉、按钮、关键图标与特殊字。
- **动态文本一律 DOM**。只有不交互、不变更且已有原始矢量源的装饰字可转轮廓 SVG；手写字、水彩、纸纹、摄影和复杂渐变使用 PNG/WebP，不把位图强行矢量化。
- 从整图裁出的素材必须标明来源、裁剪区和边缘风险。没有可分离源文件时，先报告风险，不假装有“原始 SVG”。
- **输出**：T2 和必要的 T3 素材清单/文件。素材清单与页面拆解互相引用，不重复写样式。

### 3. 开发与一次校验

- 使用语义化 HTML、Flex/Grid 和共享 CSS 变量；绝不以整页截图作为页面背景或用热点覆盖来伪造页面。
- 每个可变字段集中为 mock data 或清晰的 `data-*`/DOM 标记，方便未来接接口；装饰素材与业务数据分离。
- 在**设计图原始画布尺寸、相同滚动位置和相同页面状态**下进行一次截图对比。记录明显偏差和无法验证项。
- 标准还原到此停止：若 A 页仍有影响整体观感的差距，输出精修建议、预期收益和所缺素材，等待用户决定是否进入精修。

### 4. 精修验收（仅明确开启）

- 为指定 A 页补齐完整 T1–T4，优先修复布局、尺寸、排版和核心素材，再处理低影响细节。
- 评分只基于同视口、同状态、同数据的可见区域：布局与间距 35%、字体排版 20%、素材与插画 20%、表面样式 15%、细节/状态 10%。目标 ≥90 分；没有原始素材或无法测量的项应标为“受限”，不伪造分数。
- 每轮修复都须有新的可观察对比或明确问题被消除的证据。若继续迭代的收益很低、缺少源素材或用户没有授权投入，停止并记录阻塞；不设机械的“三轮必须结束”规则。

## 成本控制规则

1. **一次解析，多次复用**：T0/T1/T2 保存在项目中，代码和后续修改只读这些结论；不要反复从原图重新推导。
2. **先共性后特例**：同类组件确认一次，后续页面引用它，不重复分析。
3. **先高感知区域**：标准模式不分析不可见的长页尾部、未提供的状态或没有交互证据的动效。
4. **素材按需生成**：只为能明显提升还原度、且无法用 CSS/现有素材稳定实现的元素提取或生成素材。
5. **聊天保持短，文件可追溯**：在对话中报告结论、风险和下一步；详细规格只写入必要文件，避免同一信息在多份 Markdown 重复。

## 素材格式规则

| 元素来源/特征 | 实现方式 |
|---|---|
| 纯色、边框、规则圆角、简单渐变 | CSS |
| 原始矢量图标、Logo、规则图形 | SVG，保留 `viewBox` |
| 静态且已有矢量源的装饰文字 | 轮廓 SVG，可访问名称另存于 HTML |
| 手写字、水彩、纹理、摄影、复杂滤镜 | PNG/WebP；需要透明时使用透明底 |
| 可变业务文字/数字/状态 | DOM，不导出图片或转轮廓 |

不要声称能从普通截图“自动检测”字体、素材类型或原始图层；只能基于可见证据作推断，并把不确定性记为风险。

## 最小质量门槛

| Gate | 放行条件 |
|---|---|
| G0 | 文件、目标视口、页面状态、A/B/C 分级及主要不确定项已记录 |
| G1 | 共享 Token 或页面局部 Token 有唯一来源；不重复建文档 |
| G2 | 可见功能区、动态字段、核心素材和页面特例已有对应实现方案 |
| G3 | HTML 结构与 T2 区块对应；没有整图背景式伪还原 |
| G4 | 已做一次同视口对比；偏差、风险和是否建议精修均已记录 |
| G5（精修） | 有对比证据支持 ≥90 分，或明确列出受限项与原因 |

## 异常与边界

- 图片损坏、版本/页面范围不清：停在 G0，列出缺失项。
- 只有整图：可继续标准还原，但对裁图素材标记风险；没有用户确认不擅自把目标降到 85%。
- 素材无法可靠分离：使用可接受的位图裁图或 CSS 替代，并在 T3/T4 说明。
- 非设计实现任务、纯流程咨询：不运行本 skill。

## 参考文件

- `references/templates.md`：按模式使用 T0–T4 的最小模板。只读取当前模式需要的模板字段。
